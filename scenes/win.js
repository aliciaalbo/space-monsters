add([
    text('congratulations'),
    origin('center'),
    scale(2),
    pos(width()/2, height()/5)
])

add([
    text(args.score),
    origin('center'),
    scale(10),
    pos(width()/2, height()/1.3)
])